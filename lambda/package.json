{
  "name": "audio-nudge-prototype",
  "version": "1.2.0",
  "description": "An amazon alexa skill that searches the web using SerpApi, and plays an audio nudge after the answer to reduce information uncertainty",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amazon Alexa",
  "license": "Apache License",
  "dependencies": {
    "ask-sdk-core": "^2.7.0",
    "ask-sdk-model": "^1.19.0",
    "aws-sdk": "^2.326.0",
    "dotenv": "^16.4.5"
  }
}